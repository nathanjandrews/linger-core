proc main() {
  print("recursive sum: ");
  print(sum_recursive(10));
  print(", ");
  print("algebraic sum: ");
  print(sum_algebraic(10));
}

proc sum_recursive(n) {
  if (n == 1) {
    return 1;
  } else {
    return n + sum_recursive(n-1);
  }
}

proc sum_algebraic(n) {
  return n*(n+1)/2;
}