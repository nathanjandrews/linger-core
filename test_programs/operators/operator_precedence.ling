proc main() {
  print(
    2 + 3 * 7,                     // 23
    4 * 3 / 8,                     // 1.5
    true && false || false,        // false
    false && false || true,        // true 
    5 > 2 && true,                 // true
    4 * 7 < 12 * 2 || 12 + 5 >= 3  // true
  );
}