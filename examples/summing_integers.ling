proc main() {
  print("recursive sum: ");
  print(sum_recursive(n));
  print("\n);
  print("algebraic sum: ");
  print(sum_algebraic(n));
}

proc sum_recursive(n) {
  if (n == 1) {
    return 1;
  } else {
    return n + sum_recursive(n-1);
  }
}

proc sum_algebraic(n) {
  return n*(n+1)/2;
}